/* eslint-disable jsdoc/require-file-overview */
/**
 * Creating a sidebar enables the creation of an ordered group of docs, render a
 * sidebar for each doc of the group, and provide next/previous navigation.
 * Sidebars can be generated from the filesystem, or explicitly defined here.
 */

const sidebars = {
  // By default, Docusaurus generates a sidebar from the docs folder structure.
  componentsSidebar: [{ dirName: '.', type: 'autogenerated' }],
};

module.exports = sidebars;
